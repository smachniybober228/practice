<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Draft">
        <activity
            android:name=".Views.ResultActivity"
            android:exported="false"
            android:label="@string/title_activity_result"
            android:theme="@style/Theme.Draft" />
        <activity
            android:name=".Views.SecondInputActivity"
            android:exported="false"
            android:label="@string/title_activity_second_input"
            android:theme="@style/Theme.Draft" />
        <activity
            android:name=".Views.MainInputActivity"
            android:exported="false"
            android:label="@string/title_activity_main_input"
            android:theme="@style/Theme.Draft" />
        <activity android:name=".Views.HistoryActivity" />
        <activity android:name=".Views.DetailActivity" />
        <activity
            android:name=".Views.RegistrationActivity"
            android:exported="false"
            android:label="@string/title_activity_registration"
            android:theme="@style/Theme.Draft" />
        <activity
            android:name=".Views.MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Draft">
        </activity>
        <activity android:name=".Views.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>